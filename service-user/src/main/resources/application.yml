server:
  port: 7001
  tomcat:
    uri-encoding: utf-8
    max-swallow-size: -1
spring:
  application:
    name: company-service
  profiles:
    active: dev
# 配置日志级别
logging:
  level:
    root: info
# mybatis-plus
mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  global-config:
    db-config:
      id-type: assign_id